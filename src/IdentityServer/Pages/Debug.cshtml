@page
@model IdentityServer.Pages.Debug.DebugModel

<h1>All Claims in Cookie</h1>
<table>
    <tr><th>Type</th><th>Value</th></tr>
    @foreach (var claim in (IEnumerable<dynamic>)ViewData["Claims"]!)
    {
        <tr><td>@claim.Type</td><td>@claim.Value</td></tr>
    }
</table>